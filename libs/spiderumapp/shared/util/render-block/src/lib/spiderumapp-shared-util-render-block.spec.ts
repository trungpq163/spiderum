import { renderBlock } from './spiderumapp-shared-util-render-block';

describe('renderBlock', () => {
  it('should work', () => {
    expect(renderBlock()).toEqual('spiderumapp-shared-util-render-block');
  });
});
