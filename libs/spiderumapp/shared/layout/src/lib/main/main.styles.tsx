import tw from 'twin.macro';
import styled from 'styled-components';

export const Container = styled.div`
  ${tw``}
`;

export const Main = styled.main`
  ${tw`container flex flex-col mx-auto mt-13 gap-y-5`}
`;
