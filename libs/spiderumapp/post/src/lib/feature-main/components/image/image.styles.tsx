import tw from 'twin.macro';
import styled from 'styled-components';

export const ImageWrapper = styled.div``;

export const Image = styled.img``;

export const Caption = styled.p`
  ${tw`text-center text-12 text-sub-black`}
`;
